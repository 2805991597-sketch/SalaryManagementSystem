<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mapper.RoleMapper">

    <select id="list" resultType="com.example.pojo.Role">
        SELECT id, role_name AS roleName, role_code AS roleCode, remark FROM role
    </select>

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        INSERT INTO role(role_name, role_code, remark)
        VALUES(#{roleName}, #{roleCode}, #{remark})
    </insert>

    <update id="update">
        UPDATE role SET
                      role_name = #{roleName},
                      role_code = #{roleCode},
                      remark = #{remark}
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM role WHERE id = #{id}
    </delete>

    <select id="findById" resultType="com.example.pojo.Role">
        SELECT id, role_name AS roleName, role_code AS roleCode, remark FROM role WHERE id=#{id}
    </select>

    <select id="findByCode" resultType="com.example.pojo.Role">
        SELECT id, role_name AS roleName, role_code AS roleCode, remark FROM role WHERE role_code=#{code}
    </select>

</mapper>
